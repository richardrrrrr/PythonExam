[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "real-estate-crawler"
version = "0.1.0"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["rec"]
